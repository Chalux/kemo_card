{
  "name": "kemo-card-puerts",
  "version": "1.0.0",
  "description": "Kemo Card PuerTS agent TypeScript builtins",
  "private": true,
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.4.0"
  }
}
