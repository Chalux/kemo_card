---
description: 项目基础结构与编码约定（组合优先、Src/Doc/Config/Resource 目录布局）
alwaysApply: true
---

# 项目基础规则

## 设计原则

- **组合优先于继承**：用节点组合、小模块协作、依赖注入或可复用组件拼装行为；避免过深的类型继承链。新增能力时优先「持有并委托」而非「子类化一切」。

- GDScript / 场景：用子节点与 `Node` 组合承载能力，脚本用「持有引用 + 调用」扩展行为，而非层层 `extends`。
- C#（Mono）：优先通过组合小型服务类、接口与委托组装逻辑，避免仅为了复用代码而加深继承层次。

- **代码只使用C#来写，不使用GDScript**

## 本地化

- **所有面向用户的文本内容都必须使用本地化**（翻译键），禁止在场景、脚本中硬编码可见文案。
- **日志除外**：`GD.Print` / `PushWarning` / 调试输出等可直接使用明文。
- 场景控件的 `text` 等属性填写翻译键（如 `UI_MENU_TITLE`），由 Godot 自动翻译；C# 非 Node 场景用 `Localization.Tr(key)`。
- 新增文案时同步写入 `Resource/Locale/strings.csv`（及需要时的 mod 翻译 CSV）。

## 目录布局

| 用途 | 路径 |
|------|------|
| 代码根目录 | `Src/` |
| 框架 / 基础设施（通用运行时、抽象层） | `Src/frame/` |
| 业务功能模块 | `Src/mod/` |
| 工具类、纯静态辅助 | `Src/utils/` |
| 引擎补丁、对 Godot 的 workaround | `Src/fixed/` |
| 文档 | `Doc/` |
| 配置文件及打包输出 | `Config/` |
| 资源（贴图、音频、场景资源等） | `Resource/` |

新增文件时按上表归类；不确定时优先放在 `Src/mod/` 并在 PR/说明中注明，或拆成可组合的小模块再放入 `Src/frame/` / `Src/utils/`。

## 模块依赖

- **`Src/frame/` 可以引用 Godot**（节点、资源加载、`AudioServer` 等引擎 API 均可），与 `UIManager` 等现有 frame 组件一致。
- **`Src/frame/` 不得依赖或调用 `Src/mod/`**：框架层保持业务无关；禁止引用 `KemoCard.Mod.*` 命名空间、加载业务 mod 场景/内容路径，或直接调用 mod 内类型与方法。依赖方向只能是 `mod` → `frame`（以及 `utils` / `fixed`），不能反向。
- **`Src/fixed/`**：仅放对 Godot / 引擎的补丁与 workaround；通用可复用运行时能力优先放 `frame`，不要仅因「用了 Godot」就放进 `fixed`。

## Git 提交规范

详见独立规则 `git-commit-message.mdc`：提交信息必须使用简体中文。
