---
description: 确认文件改动完成后，必须再格式化一遍代码文件
alwaysApply: true
---

# 改动完成后格式化

- **时机**：对某个代码文件的逻辑改动已确定完成（不再继续修改该文件）之后，必须再对该文件执行一次格式化。
- **范围**：仅格式化本次实际改动过的代码文件（如 `.cs`）；不要批量格式化无关文件。
- **方式**：使用项目/编辑器可用的格式化手段（如 `dotnet format`、编辑器 Format Document）；格式化后若仅产生空白/风格差异，保留即可，无需再改逻辑。
- **顺序**：先完成功能与逻辑 → 确认该文件无需再改 → 再格式化 → 再继续其他文件或收尾验证。
